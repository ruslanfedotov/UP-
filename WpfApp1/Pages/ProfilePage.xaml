<Page x:Class="WpfApp1.Pages.ProfilePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Личная карточка" Background="#F0F2F5">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="White" Padding="25" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Width="80" Height="80" Background="#3498DB" Margin="0,0,20,0">
                        <TextBlock Text="👤" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock x:Name="NameTextBlock" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,5"/>
                        <TextBlock x:Name="LoginTextBlock" FontSize="13" Foreground="#7F8C8D" Margin="0,2"/>
                        <TextBlock x:Name="EmailTextBlock" FontSize="13" Foreground="#7F8C8D" Margin="0,2"/>
                        <TextBlock x:Name="RoleTextBlock" FontSize="12" Foreground="#3498DB" Margin="0,5,0,0" FontWeight="SemiBold"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="1" Background="White" Padding="20" Margin="0,0,0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="ApplyAuthorButton" Grid.Column="0" Content="✍️ Запросить статус автора" 
                            Click="ApplyAuthorButton_Click" 
                            Background="#E67E22" Foreground="White" BorderThickness="0"
                            Padding="15,10" Cursor="Hand" FontSize="13" FontWeight="SemiBold"
                            Margin="0,0,10,0"/>

                    <Button x:Name="EditProfileButton" Grid.Column="1" Content="✏️ Редактировать профиль" 
                            Click="EditProfileButton_Click" 
                            Background="#3498DB" Foreground="White" BorderThickness="0"
                            Padding="15,10" Cursor="Hand" FontSize="13" FontWeight="SemiBold"
                            Margin="10,0,0,0"/>
                </Grid>
            </Border>

            <Border Grid.Row="2" Background="White" Padding="20">
                <StackPanel>
                    <TextBlock Text="📝 Мои рецензии" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>

                    <DataGrid x:Name="ReviewsDataGrid" AutoGenerateColumns="False" IsReadOnly="True"
                              BorderBrush="#E5E5E5" RowBackground="White" AlternatingRowBackground="#F8F9FA"
                              RowHeight="45" FontSize="13" HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="📅 Дата" Binding="{Binding ReviewDate, StringFormat=\{0:dd.MM.yyyy\}}" Width="100"/>
                            <DataGridTextColumn Header="📖 Книга" Binding="{Binding Book.Title}" Width="200"/>
                            <DataGridTemplateColumn Header="⭐ Оценка" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Rating, StringFormat='⭐ {0}'}" Foreground="#F39C12" FontWeight="Bold"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="💬 Отзыв" Binding="{Binding ReviewText}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <TextBlock x:Name="NoReviewsText" Text="У вас пока нет рецензий" 
                               Foreground="#7F8C8D" FontSize="13" HorizontalAlignment="Center" 
                               Margin="0,20,0,0" Visibility="Collapsed"/>
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>